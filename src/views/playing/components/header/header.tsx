import styles from './header.module.css';

export const Header = () => {
  return (
    <header className={styles.header}>
      <h1>A MEMORY GAME</h1>
      <p>ANIMALS</p>
    </header>
  );
};

export default Header;
